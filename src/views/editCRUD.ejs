<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Bootstrap demo</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-Zenh87qX5JnK2Jl0vWa8Ck2rdkQ2Bzep5IDxbcnCeuOxjzrPF/et3URy9Bv1WTRi"
      crossorigin="anonymous"
    />
  </head>
  <body>
    <div class="container">
      <form class="row g-3" action="/put-crud" method="POST">
        <div class="col-12">Edit user's info</div>
        <div class="col-md-6">
          <label for="inputFirstName" class="form-label">First name</label>
          <input
            type="text"
            class="form-control"
            id="inputFirstName"
            name="firstName"
            value="<%= userData.firstName %>"
          />
        </div>
        <div class="col-md-6">
          <label for="inputLastName" class="form-label">Last name</label>
          <input
            type="text"
            class="form-control"
            id="inputLastName"
            name="lastName"
            value="<%= userData.lastName %>"
          />
        </div>
        <div class="col-12">
          <label for="inputAddress" class="form-label">Address</label>
          <input
            type="text"
            class="form-control"
            id="inputAddress"
            placeholder="Enter address"
            name="address"
            value="<%= userData.address %>"
          />
        </div>
        <div class="col-md-6">
          <label for="phoneNumber" class="form-label">Phone Number</label>
          <input
            type="text"
            class="form-control"
            placeholder="Enter phone number"
            name="phoneNumber"
            id="phoneNumber"
            value="<%= userData.phoneNumber %>"
          />
        </div>
        <div class="col-md-4">
          <label for="inputState" class="form-label">Gender</label>
          <select id="inputState" class="form-select" name="gender" >
            <option value="1" <%= userData.roleId == '1' ? 'selected' : ''%>>Male</option>
            <option value="2" <%= userData.roleId == '2' ? 'selected' : ''%>>Female</option>
            <option value="3" <%= userData.roleId == '3' ? 'selected' : ''%>>Other</option>
          </select>
        </div>
        <div class="col-md-2">
          <label for="inputZip" class="form-label">Role</label>
          <select id="inputState" class="form-select" name="roleId" value="<%= userData.roleId %>">
            <option value="R1" <%= userData.roleId == 'R1' ? 'selected' : ''%>>Admin</option>
            <option value="R2" <%= userData.roleId == 'R2' ? 'selected' : ''%>>Staff</option>
            <option value="R3" <%= userData.roleId == 'R3' ? 'selected' : ''%>>Customer</option>
          </select>
        </div>
        <input name="id" value="<%= userData.id %>" hidden type="text"/>
        <div class="col-12">
          <button type="submit" class="btn btn-primary">Update</button>
        </div>
      </form>
    </div>
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-OERcA2EqjJCMA+/3y+gxIOqMEjwtxJY7qPCqsdltbNJuaOe923+mo//f6V8Qbsw3"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
